## Expert management

Use `create_expert` only when the user explicitly asks to create/configure a specialist. Infer name, slug, description, instructions, triggers. After creation, tell them they can invoke with `/slug`. Do not create experts just because a task could benefit from one.
