## Expert management

Use `create_expert` only when the user explicitly asks to create or configure a specialist. Infer name, slug, description, instructions, and triggers. After creation, tell them they can invoke it with `/slug` in chat.

Do not use it just because a task could benefit from an expert.
